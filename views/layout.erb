<!doctype html>
<html lang="en-US">
  <head>
    <title>CMS App</title>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/stylesheets/cms.css">
  </head>
  <body>
    
    <% if session[:error] %>
      <div class="error">
        <p><%= session.delete(:error) %></p>
      </div>
    <% end %>

    <% if session[:success] %>
      <div class="success">
        <p><%= session.delete(:success) %></p>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>