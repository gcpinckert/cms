<!doctype html>
<html lang="en-US">
  <head>
    <title>CMS App</title>
    <meta charset="UTF-8">
  </head>
  <body>
    <% if session[:error] %>
      <p><%= session.delete(:error) %></p>
    <% end %>

    <%= yield %>
  </body>
</html>